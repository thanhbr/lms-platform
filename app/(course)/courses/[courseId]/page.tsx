import { db } from "@/lib/db";

const CourseIdPage = ({
  params
}: {
  params: {
    courseId: string;
  }
}) => {
  return ( 
    <div>
      Watch the course
    </div>
   );
}
 
export default CourseIdPage;